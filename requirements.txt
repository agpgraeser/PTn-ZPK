fastapi>=0.110.0
uvicorn[standard]>=0.29.0
numpy>=1.26.0
pydantic>=2.0.0
openpyxl>=3.1.0
python-multipart>=0.0.9
pytest>=8.0.0
httpx>=0.27.0

# Gemeinsamer Fachkern der Programmfamilie (enthaelt die ZPK-Rechnung).
# Bezug als installierbares Paket von GitHub (funktioniert lokal UND auf Render).
# Fuer lokale Kern-Entwicklung stattdessen editierbar:  pip install -e ../agp_control_kern
agp-control-kern @ git+https://github.com/agpgraeser/agp_control_kern.git@main
